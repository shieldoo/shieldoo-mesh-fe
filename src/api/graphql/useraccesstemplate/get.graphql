query UserAccessTemplate($userAccessTemplateId: Int!) {
  userAccessTemplate(id: $userAccessTemplateId) {
    id
    name
    description
    groups {
      id
      name
      description
    }
    fwConfig {
      id
      name
    }
    validTo
  }
}